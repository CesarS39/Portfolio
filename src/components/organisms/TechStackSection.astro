---
import BaseSection from "../atoms/BaseSection.astro";
import Heading from "../atoms/Heading.astro";
import TechnologyButton from "../molecules/TechnologyButton.astro";

interface Technology {
  label: string;
  icon: string;
  gradient: string;    
  isEmoji?: boolean;
}

const { techs } = Astro.props as { techs: Technology[] };
---

<BaseSection id="tecnologias" >
  <Heading level="h2" className="mb-10">
    Tecnologías
  </Heading>

  <div class="flex flex-wrap justify-center gap-4 max-w-5xl mx-auto">
    {techs.map((tech) => (
      <TechnologyButton
        icon={tech.icon}
        label={tech.label}
        gradient={tech.gradient}
        isEmoji={tech.isEmoji}
      />
    ))}
  </div>
</BaseSection>