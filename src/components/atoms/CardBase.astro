---
const { className = "" } = Astro.props;
---

<div class={`bg-surface border border-border rounded-sm p-6 transition-transform duration-300 ease-out hover:-translate-y-1 ${className}`}>
  <slot />
</div>